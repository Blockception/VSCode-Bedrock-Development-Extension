import * as vscode from "vscode";
import * as CompletionFunction from "../../general/CompletionFunctions";

export function CompletionItems(PrefixText: string, position: vscode.Position, receiver: vscode.CompletionList): void {
    
}
