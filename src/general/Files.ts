import * as vscode from "vscode";

function CollectFiles(dir : string, files: string[], directories: string[]) : void {
    var files = new Array<string>();
    var directories = new Array<string>();


}