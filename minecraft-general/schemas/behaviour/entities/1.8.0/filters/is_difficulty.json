{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "minecraft.behaviour.entities.1.8.0.filters.is_difficulty",
    "type": "object",
    "title": "Filters is_difficulty 1.8.0",
    "additionalProperties": false,
    "description": "Tests the current difficulty level of the game.",
    "required": [ "value" ],
    "properties": {
        "operator": {
            "$ref": "base_operator.json"
        },
        "subject": {
            "$ref": "base_subject.json"
        },
        "value": {
            "$ref": "base_value.json",
            "description": "The game's difficulty level to test",
            "type": "string",
            "enum": [
                "easy",
                "hard",
                "normal",
                "peacefull"
            ]
        }
    }
}