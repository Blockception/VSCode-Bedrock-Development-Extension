{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "minecraft.behaviour.entities.1.8.0.filters.moon_intensity",
    "type": "object",
    "title": "Filters moon_intensity 1.8.0",
    "additionalProperties": false,
    "description": "Compares the current moon intensity with a float value in the range (0.0, 1.0)",
    "required": [ "value" ],
    "properties": {
        "operator": {
            "$ref": "base_operator.json"
        },
        "subject": {
            "$ref": "base_subject.json"
        },
        "value": {
            "$ref": "base_value.json",
            "description": "A floating point value.",
            "type": "number",
            "minimum": 0,
            "maximum": 1
        }
    }
}