{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "minecraft.behaviour.entities.1.8.0.filters.is_block",
    "type": "object",
    "title": "Filters is_block 1.8.0",
    "additionalProperties": false,
    "description": "Returns true when the block has the given name.",
    "required": [ "value" ],
    "properties": {
        "operator": {
            "$ref": "base_operator.json"
        },
        "subject": {
            "$ref": "base_subject.json"
        },
        "value": {
            "$ref": "base_value.json",
            "description": "The Family name to look for",
            "type": "string"
        }
    }
}