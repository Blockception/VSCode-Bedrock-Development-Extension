{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "minecraft.behaviour.entities.1.8.0.minecraft.behavior.nearest_attackable_target",
  "type": "object",
  "title": "minecraft:behavior.nearest_attackable_target 1.8.0",
  "$ref": "../types/behaviour_base.json",
  "required": [],
  "properties": {
    "entity_types": { "$ref": "../types/entity_types1.8.0.json" },
    "within_radius": {
      "type": "number",
      "default": 0.0,
      "description": "Distance in blocks that the target can be within to launch an attack"
    },
    "attack_interval": {
      "type": "integer",
      "default": 0,
      "description": "Time in seconds between attacks"
    },
    "must_see": {
      "type": "boolean",
      "default": false,
      "description": "If true, only entities in this mob's viewing range can be selected as targets"
    },
    "must_see_forget_duration": {
      "type": "number",
      "default": 3.0,
      "description": "Determines the amount of time in seconds that this mob will look for a target before forgetting about it and looking for a new one when the target isn't visible any more"
    },
    "must_reach": {
      "type": "boolean",
      "default": false,
      "description": "If trueonly entities that this mob can path to can be selected as targets"
    },
    "reselect_targets": {
      "type": "boolean",
      "default": false,
      "description": "If true the target will change to the current closest entity whenever a different entity is closer"
    },
    "scan_interval": {
      "type": "integer",
      "default": 10,
      "description": "How many ticks to wait between scanning for a target."
    },
    "target_search_height": {
      "type": "number",
      "default": -1.0,
      "description": "Height in blocks to search for a target mob. -1.0f means the height does not matter."
    },
    "persist_time": {
      "type": "number",
      "default": 0.0,
      "description": "Time in seconds for a valid target to stay targeted when it becomes and invalid target."
    }
  }
}