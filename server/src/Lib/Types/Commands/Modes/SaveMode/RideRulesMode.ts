import { ModeCollection } from "../Interface";

export const SaveMode: ModeCollection = {
  Name: "Save Mode",
  Modes: [
    { Name: "disk", Description: "Saves to the disk" },
    { Name: "memory", Description: "Saves to memory" },
  ],
};
